#include <stdio.h>
void main()
{
    char str[] = "Believe in yourself";
    int sum=0;
    for (int i = 0; str[i] != '\0'; i++)
    {
        sum  = i;
    }

    printf("\n count is: %d",sum+1);
}

